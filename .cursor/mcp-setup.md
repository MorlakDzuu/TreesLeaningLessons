# Настройка MCP серверов для обучения

## ✅ Установленные серверы

### 1. LeetCode MCP Server
**Статус**: ✅ Установлен

**Пакет**: `@jinzcdev/leetcode-mcp-server`

**Возможности**:
- Получение задач LeetCode по тегам (trees, binary-tree, etc.)
- Проверка решений локально
- Получение примеров входных/выходных данных
- Отслеживание прогресса решения задач

**Использование**:
```bash
# Сервер уже установлен глобально
# Используется через npx в конфигурации
```

### 2. Graphviz
**Статус**: ✅ Установлен (через Homebrew)

**Возможности**:
- Генерация диаграмм деревьев из DOT-кода
- Создание визуализаций обходов
- Экспорт в PNG/SVG

**Установлено**:
- Graphviz (через Homebrew): `dot -V` показывает версию 14.1.1

**Использование**:
- Graphviz используется **напрямую через команду `dot`**
- AI может вызывать команды терминала для генерации изображений
- Не требует отдельного MCP сервера

**Пример использования AI**:
```bash
# AI может выполнять команды типа:
echo 'digraph BST { 5 -> 3; 5 -> 7; }' | dot -Tpng -o tree.png
```

---

## 🔧 Настройка в Cursor

### Вариант 1: Через настройки Cursor (рекомендуется)

1. Откройте настройки Cursor:
   - `Cmd + ,` (macOS) или `Ctrl + ,` (Windows/Linux)
   - Или через меню: `Cursor` → `Settings`

2. Найдите раздел "MCP Servers" или "Model Context Protocol"

3. Добавьте конфигурацию из файла `.cursor/mcp-config.json`

### Вариант 2: Через файл конфигурации

Конфигурация находится в файле: `.cursor/mcp-config.json`

**Важно**: В Cursor конфигурация MCP может находиться в:
- `~/.cursor/mcp.json` (глобальная настройка)
- Или в настройках проекта

---

## 📝 Конфигурация серверов

### LeetCode Server

```json
{
  "leetcode": {
    "command": "npx",
    "args": ["-y", "@jinzcdev/leetcode-mcp-server"],
    "env": {
      "LEETCODE_SITE": "global"
    }
  }
}
```

**Опционально**: Для доступа к личному профилю добавьте:
```json
"env": {
  "LEETCODE_SITE": "global",
  "LEETCODE_SESSION": "ваш_session_cookie"
}
```

### Graphviz

Graphviz **не требует MCP сервера** - используется напрямую через команду `dot`.

AI может вызывать Graphviz через терминал для генерации изображений:

```bash
# Пример команды, которую AI может выполнить:
echo 'digraph BST {
    node [shape=circle];
    5 -> 3;
    5 -> 7;
    3 -> 2;
    3 -> 4;
}' | dot -Tpng -o tree.png
```

**Преимущества**:
- ✅ Не требует дополнительной настройки
- ✅ Работает сразу после установки Graphviz
- ✅ Проще и надёжнее, чем MCP сервер

---

## 🚀 Использование

### Пример 1: Получение задачи LeetCode

```
AI: "Получи задачу Validate Binary Search Tree из LeetCode"
```

MCP сервер вернёт:
- Условие задачи
- Примеры входных/выходных данных
- Ограничения

### Пример 2: Визуализация дерева

```
AI: "Создай визуализацию этого BST: [5, 3, 7, 2, 4, 6, 8]"
```

MCP сервер создаст DOT-код и сгенерирует изображение.

---

## 🐛 Устранение проблем

### LeetCode сервер не работает

**Симптомы**: Сервер не запускается, ошибки в логах

**Решение**:

1. **Проверьте установку**:
```bash
npm list -g @jinzcdev/leetcode-mcp-server
```

2. **Переустановите при необходимости**:
```bash
npm install -g @jinzcdev/leetcode-mcp-server
```

3. **Проверьте Node.js**:
```bash
node --version
npm --version
```

4. **Используйте npx** (уже настроено в конфигурации):
   - Конфигурация использует `npx -y`, что автоматически скачает пакет при необходимости

### Graphviz не работает

**Симптомы**: Команда `dot` не найдена, ошибки при генерации изображений

**Решение**:

1. **Проверьте установку**:
```bash
dot -V
# Должно показать версию
```

2. **Если не установлен, установите**:
```bash
brew install graphviz  # macOS
# или
sudo apt install graphviz  # Linux
```

3. **Проверьте DOT-код**:
   - Убедитесь, что DOT-код синтаксически правильный
   - Проверьте через команду: `dot -Tpng input.dot -o output.png`

### Cursor не видит MCP серверы

**Симптомы**: Серверы не отображаются в настройках, не работают

**Решение**:

1. **Проверьте конфигурацию**:
   - Откройте настройки Cursor (`Cmd + ,`)
   - Найдите раздел "MCP Servers"
   - Убедитесь, что конфигурация добавлена из `mcp-config.json`

2. **Перезапустите Cursor**:
   - После изменения конфигурации обязательно перезапустите Cursor

3. **Проверьте логи**:
   - В Cursor откройте Developer Tools
   - Проверьте логи на наличие ошибок

4. **Проверьте пути**:
   - Убедитесь, что все пути в конфигурации правильные
   - Для команд используйте полные пути или убедитесь, что они в PATH

---

## ✅ Проверка работоспособности

### Тест LeetCode MCP:

```bash
# Запустите сервер вручную
npx -y @jinzcdev/leetcode-mcp-server
```

### Тест Graphviz:

```bash
# Создайте тестовый DOT файл
echo 'digraph G { A -> B; }' > test.dot

# Сгенерируйте изображение
dot -Tpng test.dot -o test.png

# Проверьте, что файл создан
ls -la test.png
```

---

## 📝 Полезные команды

### Проверка установки:

```bash
# Graphviz
dot -V
which dot

# LeetCode MCP
npm list -g @jinzcdev/leetcode-mcp-server

# Node.js
node --version
npm --version
```

### Переустановка:

```bash
# Graphviz (macOS)
brew reinstall graphviz

# LeetCode MCP
npm install -g @jinzcdev/leetcode-mcp-server
```

---

## 📚 Дополнительные ресурсы

- [Model Context Protocol Documentation](https://modelcontextprotocol.io/)
- [LeetCode MCP Server](https://www.npmjs.com/package/@jinzcdev/leetcode-mcp-server)
- [Graphviz Documentation](https://graphviz.org/)

---

## 💡 Рекомендации

1. **Для LeetCode**: Начните с базовых задач, используйте MCP для получения условий
2. **Для Graphviz**: Используйте для визуализации при изучении новых структур данных
3. **Комбинируйте**: Используйте оба сервера вместе для полного цикла обучения

---

**Примечание**: Если Graphviz MCP сервер не работает напрямую, AI может использовать Graphviz через команду `dot` для генерации диаграмм.

